# 1Password 专项节点选择规则。
# 维护原则：
# 1) 只收敛 1Password 官方明确公布的核心连接域名与更新/基础设施端点。
# 2) 上游快照由 tools/sync_upstream_rules.py 每日抓取官方支持页自动刷新。
# 3) 不自动并入 Watchtower、Fastmail、Brex、Privacy Cards 等第三方依赖域名。
# 4) 客户端接入时必须放在 proxy/gfw 前；工作白名单里也应作为显式放行入口。

INCLUDE,upstream/onepassword/core_domains.list
